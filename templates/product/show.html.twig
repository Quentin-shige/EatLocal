{% extends 'base.html.twig' %}

{% block title %}{{product.name}} - EatLocal{% endblock %}

{% block body %}
<h1>{{product.name}}</h1><br>

    <div class="row d-flex justify-content-center text-center">
        <div class="col-md-4">
            <div class="card-product text-center">
                <a href="{{path('product', { 'slug' : product.slug })}}">
                <img src="/uploads/{{product.illustration}}" alt="{{ product.name}}" class="img-fluid">
                </a>
                <h4>{{product.name}}</h4>
                <hr>
                <p>{{product.description}}</p>
                <span>{{ (product.price /100)|number_format(2, ',', '.')}} €</span>
                <hr>
            </div>
                <a href={{ path("add_to_cart", {"id": product.id})}} class="btn-orange-primary "> Ajouter au panier</a>
        </div>
    </div>
    
  
<hr>
{% include "product/bestproduct.html.twig" %}

{% endblock %}
